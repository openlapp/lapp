{
  // LAPP schema version.
  "schemaVersion": "1.0",

  // Applications may use this when they do not have their own default model.
  "defaultModel": {
    "providerId": "deepseek",
    "model": "deepseek-v4-flash"
  }
}
